import MdxLayout from "@/components/mdx-layout"
import S3GLogo from "@/components/S3GLogo";

# Welcome to <S3GLogo/> – A Smarter Gallery

**s3g** is a modern gallery platform built with S3 storage, **BetterAuth** for secure RBAC, and powered by **Drizzle ORM** with neon database serverless postgres.

## 🔒 Access Roles

- **Authenticated Users**: Upload and manage your own media.
- **Guests**: Browse and view public posts.
- **Admins**: Full access to upload, remove, and manage all content.

## ✨ Features

- Fast uploads to S3
- Role-based access control with BetterAuth
- Modern database access with Drizzle + PostgreSQL

export default function MDXPage({ children }) {
  return <MdxLayout>{children}</MdxLayout>
}
